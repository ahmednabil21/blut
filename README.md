# blut

واجهة Wakeel / ISP IQ — التطبيق في مجلد `wakeelfrontend`.

```bash
npm install --prefix wakeelfrontend
npm start
```

انسخ `wakeelfrontend/.env.example` إلى `.env.development` واضبط `REACT_APP_API_URL` و`REACT_APP_BACKEND`.
